# This workflow has been superseded by ci.yml which adds lint, unit-test,
# and ephemeral schema cleanup jobs. See .github/workflows/ci.yml.
#
# Left here as a reference stub so existing branch protection rules that
# require "dbt CI" still resolve; the full logic lives in ci.yml.

name: dbt CI (legacy stub)

on:
  pull_request:
    branches: [main]
    paths:
      - "dbt_project/**"

jobs:
  redirect:
    name: See ci.yml
    runs-on: ubuntu-latest
    steps:
      - name: Redirect notice
        run: echo "Full CI pipeline is in .github/workflows/ci.yml"
